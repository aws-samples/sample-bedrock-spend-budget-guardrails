# Cost estimate

> Generated by [`scripts/estimate-cost.ts`](../scripts/estimate-cost.ts) on 2026-05-15 for region `us-west-2`. Re-run via `npm run estimate-cost` to refresh.

Every line below pulls live unit pricing from the AWS Pricing API at script-run time, then multiplies by the usage assumptions declared in the script. Treat the totals as a defensible ballpark — your actual bill will track close to this if your traffic matches the declared assumptions, but **always verify against the AWS Pricing Calculator and your own account history before committing budget.**

## Scenarios

| Scenario | Bedrock invocations / month | API requests / month | CloudFront egress | CWL ingest | S3 storage |
|---|---|---|---|---|---|
| **Light demo** | 10,000 | 5,000 | 0.5 GB | 0.5 GB | 0.5 GB |
| **Production** | 1,000,000 | 50,000 | 5 GB | 5 GB | 5 GB |

## Per-component breakdown

| Component | Driver | Unit price | Light demo | Production |
|---|---|---|---|---|
| **CloudTrail data events** | Bedrock data events | $0.0000 /event | $0.0100 | $1.00 |
| **DynamoDB on-demand writes** | ~4 writes per invocation × all tables | $0.0000 /WCU | $0.0250 | $2.50 |
| **DynamoDB on-demand reads** | ~2 reads per invocation | $0.0000 /RCU | $0.0025 | $0.2500 |
| **DynamoDB PITR** | GB-month across all tables | $0.2000 /GB-mo | $0.0100 | $0.1000 |
| **Lambda requests (Graviton)** | ~3 invocations per Bedrock request | $0.0000 /request | $0.0060 | $0.6000 |
| **Lambda duration (Graviton)** | 256 MB × 200ms × 3 fns/request | $0.0000 /GB-second | $0.0200 | $2.00 |
| **CloudWatch Logs ingest** | meter + identity-cache + Lambda logs | $0.5000 /GB | $0.2500 | $2.50 |
| **CloudWatch Logs storage** | 14d retention × ingest GB | $0.0300 /GB-mo | $0.0069 | $0.0690 |
| **CloudWatch Synthetics canary** | 30-min cadence × 24 × 30 = 1440 runs/mo | $0.0017 /run | $2.45 | $2.45 |
| **AWS Config items recorded (prod only)** | ~10k config items / mo (BBG resources + Lambda versions + IAM) | $0.0030 /item | $0.0000 | $30.00 |
| **AWS Config rule evaluations (prod only)** | 19 managed rules × ~daily evaluations | $0.0010 /eval | $0.0000 | $0.5700 |
| **WAFv2 WebACL (prod only)** | 1 WebACL | $5.00 /mo | $0.0000 | $5.00 |
| **WAFv2 managed rule groups (prod only)** | 3 rule groups: CommonRuleSet + KnownBadInputs + IpReputation | $1.00 /group/mo | $0.0000 | $3.00 |
| **WAFv2 requests (prod only)** | CloudFront → ALB requests | $0.0000 /request | $0.0000 | $0.0300 |
| **CloudFront egress** | web app assets + API JSON | $0.0850 /GB | $0.0425 | $0.4250 |
| **CloudFront HTTPS requests** | web app + API requests | $0.0000 /request | $0.0600 | $0.6000 |
| **S3 Standard (LedgerBucket + access logs)** | GB-month after IA/Glacier lifecycle | $0.0230 /GB-mo | $0.0115 | $0.1150 |
| **API Gateway HTTP API** | API requests | $0.0000 /request | $0.0050 | $0.0500 |
| **KMS CMKs** | 4 keys (data, ledger, athena results, access logs) | $1.00 /key/mo | $4.00 | $4.00 |
| **Total** | | | **$6.90** | **$55.26** |

## Sources

Each component above pulled its unit price from one of these AWS Pricing API queries:

- **CloudTrail data events**: fallback (no CloudTrail data-events SKU returned)
- **DynamoDB on-demand writes**: Amazon DynamoDB PayPerRequest + PITR SKUs in us-west-2
- **DynamoDB on-demand reads**: Amazon DynamoDB PayPerRequest + PITR SKUs in us-west-2
- **DynamoDB PITR**: Amazon DynamoDB PayPerRequest + PITR SKUs in us-west-2
- **Lambda requests (Graviton)**: AWS Lambda ARM (Graviton) request + duration SKUs in us-west-2
- **Lambda duration (Graviton)**: AWS Lambda ARM (Graviton) request + duration SKUs in us-west-2
- **CloudWatch Logs ingest**: CloudWatch Logs Ingest + Storage SKUs in us-west-2
- **CloudWatch Logs storage**: CloudWatch Logs Ingest + Storage SKUs in us-west-2
- **CloudWatch Synthetics canary**: CloudWatch Synthetics canary-run SKU in us-west-2
- **AWS Config items recorded (prod only)**: AWS Config ConfigurationItemRecorded + ConfigRuleEvaluations SKUs in us-west-2
- **AWS Config rule evaluations (prod only)**: AWS Config ConfigurationItemRecorded + ConfigRuleEvaluations SKUs in us-west-2
- **WAFv2 WebACL (prod only)**: WAFv2 (CLOUDFRONT scope, Global) WebACL + ManagedRule + Request SKUs
- **WAFv2 managed rule groups (prod only)**: WAFv2 (CLOUDFRONT scope, Global) WebACL + ManagedRule + Request SKUs
- **WAFv2 requests (prod only)**: WAFv2 (CLOUDFRONT scope, Global) WebACL + ManagedRule + Request SKUs
- **CloudFront egress**: CloudFront global egress + HTTPS requests SKUs
- **CloudFront HTTPS requests**: CloudFront global egress + HTTPS requests SKUs
- **S3 Standard (LedgerBucket + access logs)**: S3 Standard storage SKU in us-west-2
- **API Gateway HTTP API**: API Gateway HTTP API SKU in us-west-2
- **KMS CMKs**: KMS customer-managed CMK SKU in us-west-2

## Notes

- **Cognito** is excluded — the User Pool sits in the 50,000 MAU free tier for any realistic admin/operator headcount.
- **EventBridge Scheduler** is excluded — the monthly period-rollover cron is well within the EventBridge free tier.
- **Athena** is excluded — only billed when an admin runs a Reports query, and 1 GB scanned costs ~$0.005.
- **Route53 hosted zone** ($0.50/mo) is excluded because BBG reuses your existing zone, not its own.
- **ACM certificates** are free at AWS.
- The Bedrock invocations BBG **measures** are billed to the calling IAM principal exactly as they would be without BBG; they do not appear in this cost estimate.

## Cost-control levers

- Set `bbg:disableConfigStack: true` in the SSM operator config — saves the AWS Config line if you already run Config via Control Tower or Security Hub.
- Comment out `AppCanary` in `infra/lib/observability-stack.ts` to skip the Synthetics canary.
- WAFv2 is already prod-only by default; dev never gets it.
- DynamoDB PITR can be disabled on `IdentityCache`, `PendingMeter`, and `AgentSessions` (they have TTLs) for a few cents savings.
